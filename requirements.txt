# Raspberry Pi 5 Vision Detection System Requirements

# Core computer vision libraries
opencv-python==4.8.1.78
numpy==1.24.3

# Raspberry Pi camera support
picamera2==0.3.12

# Timezone support
pytz==2023.3

# Additional utilities
configparser==5.3.0

# Optional: Machine learning libraries (for advanced detection)
# tensorflow==2.13.0  # Uncomment if using TensorFlow models
# torch==2.0.1        # Uncomment if using PyTorch models
# torchvision==0.15.2 # Uncomment if using PyTorch vision

# Optional: Web interface dependencies (if needed)
# flask==2.3.3        # Uncomment for web interface
# socketio==5.8.0     # Uncomment for real-time updates

# Development and testing
pytest==7.4.0

# Note: Some packages may need to be installed via apt on Raspberry Pi:
# sudo apt update
# sudo apt install python3-opencv python3-numpy
# sudo apt install libcamera-apps  # For rpicam-still command